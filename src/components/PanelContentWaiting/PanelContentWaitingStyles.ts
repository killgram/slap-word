import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 0.9,
    justifyContent: 'center',
  },
  textContent: {
    textAlign: 'center',
  },
})
